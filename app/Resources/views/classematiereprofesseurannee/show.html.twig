{% extends 'base.html.twig' %}

{% block body %}
    <h1>Classematiereprofesseurannee</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ classeMatiereProfesseurAnnee.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('classematiereprofesseurannee_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('classematiereprofesseurannee_edit', { 'id': classeMatiereProfesseurAnnee.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
